# External Detection Logs

Place externally generated detection JSON files here when running:

```bash
python src/main.py --skip-detection --detections data/annotations/sample_detections.json --output outputs
```

The JSON shape should match `data/synthetic/synthetic_detections.json`.
